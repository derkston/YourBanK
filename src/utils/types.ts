export type TUser = {
  isLogged : boolean,
  isRegister : boolean,
  first_name : string,
  last_name : string,
  tel : number | null,
  password : number | null,
}
export type TSlide = {
		text : string ,
		person : string ,
		for : string ,
	}
